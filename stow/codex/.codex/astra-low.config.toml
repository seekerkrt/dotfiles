# ~/.codex/astra-high.config.toml
# Inherits approval, sandbox, web-search, and verbosity defaults
# from ~/.codex/config.toml.

model = "gpt-6-astra"
model_reasoning_effort = "low"
service_tier = "default"
