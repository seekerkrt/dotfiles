#autoload
clipwl() {
  emulate -L zsh
  setopt pipefail

  local wl="/usr/bin/wl-copy"
  if [[ ! -x "$wl" ]]; then
    wl="$(command -v wl-copy 2>/dev/null)"
  fi
  if [[ -z "$wl" || ! -x "$wl" ]]; then
    print -u2 "clipwl: wl-copy not found. PATH=$PATH"
    return 127
  fi

  "$wl"
}
